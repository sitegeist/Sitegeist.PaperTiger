prototype(Sitegeist.PaperTiger:SchemaCase) < prototype(Neos.Fusion:Case) {
  default {
    @position = 'end'
    condition = Neos.Fusion:CanRender {
      type = ${node.nodeType.name + '.Schema'}
    }
    type = ${node.nodeType.name + '.Schema'}
  }
}
