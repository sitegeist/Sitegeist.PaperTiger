prototype(Sitegeist.PaperTiger:FieldCollection.Renderer) < prototype(Neos.Fusion:Component) {
    class = null
    nodePath = null

    renderer = Neos.Neos:ContentCollection {
        nodePath = ${props.nodePath}
        attributes.class = ${props.class}
    }
}
