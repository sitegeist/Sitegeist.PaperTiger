prototype(Sitegeist.PaperTiger:Field.TelephoneNumber.Schema) < prototype(Neos.Fusion:Value) {
  value = ${Form.Schema.string()}
  value.@process.addIsRequired.@if.isDefined = ${q(node).property('isRequired')}
  value.@process.addIsRequired = ${value.required}
}
