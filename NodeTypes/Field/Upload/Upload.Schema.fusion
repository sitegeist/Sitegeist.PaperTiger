prototype(Sitegeist.PaperTiger:Field.Upload.Schema) < prototype(Sitegeist.FusionForm.Upload:Schema.UploadedFile) {
  allowEmpty = ${q(node).property('isRequired') ? false : true}
  allowedExtensions = ${q(node).property('allowedExtensions')}
  maximumSize = ${q(node).property('allowedFilesize')}
}
